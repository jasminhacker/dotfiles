# don't require `cd` when changing directories 
setopt AUTO_CD

# allow piping to multiple outputs
setopt MULTIOS

# spell check commands
setopt CORRECT

# don't beep
setopt NO_BEEP

# sort by number instead on alphabetical
setopt NUMERIC_GLOB_SORT

# comments in shell
setopt INTERACTIVECOMMENTS
